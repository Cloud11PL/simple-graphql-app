import styled from 'styled-components';

const StyledTd = styled.td`
  padding: 1em 0;
  text-align: right;

  &:first-of-type {
    text-align: left;
    color: #00687f;
  }
`;

export default StyledTd;
